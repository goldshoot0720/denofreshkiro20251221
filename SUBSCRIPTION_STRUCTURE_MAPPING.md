# 📋 訂閱管理系統 - 結構對照表

## 📊 Back4App 資料庫 vs 前端表單對照

### Back4App `subscription` 表結構
```json
{
  "className": "subscription",
  "fields": {
    "objectId": {"type": "String"},
    "createdAt": {"type": "Date"},
    "updatedAt": {"type": "Date"},
    "ACL": {"type": "ACL"},
    "name": {"type": "String", "required": true},
    "price": {"type": "Number", "required": false},
    "nextdate": {"type": "Date", "required": false},
    "site": {"type": "String", "required": false},
    "account": {"type": "String", "required": false},
    "note": {"type": "String", "required": false}
  }
}
```

### 前端表單欄位 (當前)
- ✅ `name` (String) - 訂閱名稱
- ❌ `category` (String) - **多餘欄位，需要移除**
- ✅ `price` (Number) - 價格
- ✅ `site` (String) - 網站 URL
- ✅ `account` (String) - 帳戶
- ✅ `nextdate` (Date) - 下次付款日期
- ✅ `note` (String) - 備註

### � 需要修復的問題

1. **多餘的 category 欄位**
   - 前端有此欄位但資料庫沒有
   - 會導致資料無法正確儲存
   - 搜尋功能依賴不存在的欄位

2. **其他前端額外欄位**
   - `currency`, `billingCycle`, `status`, `reminderDays`
   - 這些是前端計算欄位，不會傳送到資料庫

### 📝 修復方案

#### 移除 category 欄位 (推薦)
```typescript
// 移除表單中的分類欄位
// 更新搜尋邏輯，只搜尋 name 和 note
// 更新顯示邏輯，移除分類顯示
```

### 🎯 當前狀態
- **前端表單**: 6/6 核心欄位 + 1 多餘欄位
- **資料傳輸**: ⚠️ 有多餘欄位
- **功能影響**: ⚠️ 搜尋和顯示邏輯錯誤

### 📋 修復優先級
1. **高優先級**: 移除 category 欄位
2. **中優先級**: 修復搜尋邏輯
3. **低優先級**: 清理其他前端額外欄位